# Mistral CLI Toolbox

Toolbox for Mistral CLI configuration


## Install skills

Install with:
```bash
export CURRENT_FOLDER=$(pwd)
ln -s "$CURRENT_FOLDER/skills" ~/.vibe/skills
```


## Misc

For now this repo contains a few PoCs.
